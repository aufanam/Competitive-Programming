#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    // code
    int n, m, k;
    cin >> n >> m >> k;

    if (m < n || k < n)
    {
        cout << "No\n";
    }
    else
    {
        cout << "Yes\n";
    }
    // code
    return 0;
}