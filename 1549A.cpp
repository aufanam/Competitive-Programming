#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    // code
    int t, k;

    cin >> t;

    while (t--)
    {
        cin >> k;
        cout << 2 << ' ' << k - 1 << '\n';
    }
    // code
    return 0;
}